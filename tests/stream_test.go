package test

import (
	//"fmt"
	"testing"
	//"github.com/qascade/yast/cmd"
	//"github.com/stretchr/testify/require"
)

//Interface for mocking tui
// type tui interface {
// 	RenderListModelView()
// 	StartStream()
// }

func TestStreaming(t *testing.T) {
	// fmt.Println("Executing Streaming Test")
	// var query = "spiderman"
	// cmd.SearchCmd.Flags().Set("movie", query)
	// err := cmd.Search(cmd.SearchCmd, []string{"-m", query})
	// require.NoError(t, err)
}
